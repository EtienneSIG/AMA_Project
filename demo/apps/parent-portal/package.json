{
  "name": "learneu-parent-portal",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check db/index.js && node --check auth.js && node --check contentSafety.js",
    "seed": "node -e \"require('./db').init().then(()=>{console.log('schema+seed ready');process.exit(0)}).catch(e=>{console.error(e);process.exit(1)})\"",
    "digests": "node ../../scripts/send_digests.js"
  },
  "dependencies": {
    "express": "^4.21.0",
    "bcryptjs": "^2.4.3",
    "cookie-parser": "^1.4.7",
    "pg": "^8.13.0",
    "@azure/identity": "^4.4.1",
    "nodemailer": "^6.9.14"
  }
}
